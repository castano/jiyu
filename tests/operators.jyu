
#import "LibC";

struct Matrix {

}

func main() {
    var m: Matrix;
    var n: Matrix;

    // m + n;
    // m - n;
    // m / n;
    // m * n;
    // m % n;

    // m * n;

    var f1: float = 1.0;
    var f2: float;

    // f1 | f2;
    // f1 & f2;
    // f1 ^ f2;

    // f1 || f2;
    // f1 && f2;
    // f1.f2;
    // f1 % f2;

    f1 += 2;
    printf("Float: %f\n", f1);

    f1 -= 1;
    printf("Float: %f\n", f1);
}
